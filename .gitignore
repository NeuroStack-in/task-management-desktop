# Rust
/target
**/target

# Webview (Preact/Vite)
node_modules/
/ui/dist

# Tauri generated schemas (regenerated on build). The icons/ assets ARE committed — the build
# needs them; regenerate with `cargo tauri icon <logo>` if the brand mark changes.
/src-tauri/gen

# Local agent state / jsonl outbox (never commit runtime data)
*.db
*.db-shm
*.db-wal
/.agent-state
/src-tauri/.agent-state

# Env / secrets (but keep the committed example)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
