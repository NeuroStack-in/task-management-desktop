# Rust
/target
**/target

# Tauri generated schemas (regenerated on build). The icons/ assets ARE committed — the build
# needs them; regenerate with `cargo tauri icon <logo>` if the brand mark changes.
/tray/src-tauri/gen

# SQLCipher outbox / local agent state (never commit runtime data)
*.db
*.db-shm
*.db-wal
/.agent-state

# Env / secrets
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
